This sample, datatypes.fla, provides an example of converting data from one type to another.

You convert the following:
an array to a list, 
a string to an array, 
an array to a string, 
a string to an integer, 
and a string to a floating point number.

Look for the results in the Output panel.