var arr = ["a", "b", "c"],
	len = arr.length,
	counter = 0;

while (counter < len) {
	// do something

	counter++;
}


////////////////////////////


var foo = ['a', 'b', 'c'],
    len = foo.length,
    i = -1;

while(++i < len) {
	console.log(foo[i]);
}